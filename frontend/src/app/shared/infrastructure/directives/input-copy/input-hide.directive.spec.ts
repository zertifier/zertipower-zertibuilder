import { InputHideDirective } from "./input-hide.directive";

describe("InputHideDirective", () => {
	it("should create an instance", () => {
		const directive = new InputHideDirective();
		expect(directive).toBeTruthy();
	});
});
