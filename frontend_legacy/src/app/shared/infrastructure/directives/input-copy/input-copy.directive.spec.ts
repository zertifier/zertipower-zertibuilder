import { InputCopyDirective } from "./input-copy.directive";

describe("InputCopyDirective", () => {
	it("should create an instance", () => {
		const directive = new InputCopyDirective();
		expect(directive).toBeTruthy();
	});
});
